#!/usr/bin/env bash
set -e

root_path=$(realpath .)
# config_path="$(realpath ..)/python.toml"

# if [ ! -f "$config_path" ]; then
#     echo "config file not found: $config_path"
#     exit 1
# fi

sam deploy \
    --template-file "${root_path}/template.yml" \
    --stack-name "zip-lambda-py" \
    --capabilities "CAPABILITY_IAM"